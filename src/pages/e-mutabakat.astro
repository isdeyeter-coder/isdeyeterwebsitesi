---
// src/pages/e-mutabakat.astro
import BasicLayout from '../layouts/BasicLayout.astro';
import configData from './data/config.json';

const companyName = configData.company;
const siteUrl = configData.siteUrl;
const whatsappNumber = configData.whatsapp;
const whatsappText = configData.whatsappText || "WhatsApp'tan Bilgi Al";
const buttonText = configData.buttonText || "Başvur";

// WhatsApp link oluşturma fonksiyonu
function getWhatsAppLink(serviceTitle) {
  return `https://wa.me/${whatsappNumber}?text=Merhaba,%20${encodeURIComponent(serviceTitle)}%20hakkında%20bilgi%20almak%20istiyorum`;
}

const pageTitle = "e-Mutabakat Çözümleri | Dijital Mutabakat ve Fatura Sistemleri";
const pageDescription = "e-Mutabakat, e-Fatura, e-Arşiv, e-İrsaliye çözümlerimizle iş süreçlerinizi dijitalleştirin. %50 indirimli kontörlerle tasarruf edin.";
---

<BasicLayout title={pageTitle} description={pageDescription}>
  <div class="e-mutabakat-page">
    <!-- Hero Section -->
    <section class="hero-section">
      <div class="container">
        <div class="hero-content">
          <h1 class="hero-title">
            <span class="e-text">e-Mutabakat</span>
            <span class="tagline">Mutabakat süreçlerinizde artık yalnız değilsiniz!</span>
          </h1>
          <p class="hero-subtitle">
            E-Mutabakat kullanın zamandan tasarruf edin. Hızlı, güvenli ve uygun maliyetli dijital çözümler.
          </p>
          <div class="hero-actions">
            <a href="#contact" class="btn primary-btn">
              <span class="btn-icon">🚀</span>
              Hemen Kullanmaya Başla
            </a>
            <a href="#features" class="btn secondary-btn">
              <span class="btn-icon">📋</span>
              Daha Fazla Bilgi
            </a>
          </div>
        </div>
        <div class="hero-illustration">
          <div class="mutabakat-animation">
            <div class="document-icon">📄</div>
            <div class="check-icon">✅</div>
            <div class="sync-icon">🔄</div>
          </div>
        </div>
      </div>
    </section>

    <!-- Discount Banner -->
    <section class="discount-section">
      <div class="container">
        <div class="discount-content">
          <div class="discount-badge">
            <span class="discount-text">%50</span>
            <span class="discount-label">İNDİRİM</span>
          </div>
          <div class="discount-info">
            <h2>E-Fatura ve E-Arşiv kontörlerinde %50 indirim!</h2>
            <p>
              Tüm dijital dönüşüm ürünlerinde aldığın kontör kadar hediye ediyoruz. 
              Üstelik fiyatlarımızda çok iddialı! e-Fatura, e-Arşiv ve e-İrsaliye ile 
              hem tasarruf edin hem de kazançlı çıkın. 1 Gün içinde geçişinizi tamamlayın!
            </p>
          </div>
          <div class="discount-action">
            <a href={getWhatsAppLink("%50 Kontör İndirimi")} class="btn discount-btn">
              <span class="btn-icon">🎁</span>
              Hemen Teklif Al
            </a>
          </div>
        </div>
      </div>
    </section>

    <!-- About Section -->
    <section class="about-section">
      <div class="container">
        <div class="about-header">
          <h2 class="section-title">Neden bizi tercih etmelisin?</h2>
          <div class="company-info">
            <div class="company-logo">E-MUTABAKAT BİLİŞİM A.Ş</div>
            <div class="company-tagline">İŞİMİZ SADECE MUTABAKAT</div>
          </div>
        </div>

        <div class="stats-grid">
          <div class="stat-card">
            <div class="stat-number">10.000+</div>
            <div class="stat-title">Mutabakat Tamamlandı</div>
          </div>
          <div class="stat-card">
            <div class="stat-number">99.9%</div>
            <div class="stat-title">Başarı Oranı</div>
          </div>
          <div class="stat-card">
            <div class="stat-number">7 dk</div>
            <div class="stat-title">Hızlı Kurulum</div>
          </div>
          <div class="stat-card">
            <div class="stat-number">7/24</div>
            <div class="stat-title">Destek</div>
          </div>
        </div>

        <div class="warning-banner">
          <div class="warning-icon">⚠️</div>
          <div class="warning-content">
            <h3>Yanıt almadığınız mutabakatlara para ödemeyin!</h3>
            <p>E-Mutabakat sistemimiz ile yanıt alınmayan mutabakatlar için gereksiz ödemelerden kurtulun.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Models Section -->
    <section id="features" class="models-section">
      <div class="container">
        <div class="section-header">
          <h2 class="section-title">E-Mutabakat Çalışma Modelleri</h2>
          <p class="section-subtitle">İhtiyacınıza en uygun modeli seçin</p>
        </div>

        <div class="models-grid">
          <div class="model-card">
            <div class="model-icon">💻</div>
            <h3>Online Mutabakat</h3>
            <p>Kendi ekibinizle online platform üzerinden mutabakat süreçlerini yönetin.</p>
            <ul class="model-features">
              <li>Online platform erişimi</li>
              <li>Gerçek zamanlı takip</li>
              <li>Otomatik hatırlatma</li>
            </ul>
          </div>

          <div class="model-card featured">
            <div class="model-icon">✅</div>
            <h3>Yanıt Garantili Mutabakat</h3>
            <p>Mutabakatlarınızın yanıt almasını garantileyen profesyonel çözüm.</p>
            <ul class="model-features">
              <li>%100 yanıt garantisi</li>
              <li>Profesyonel takip ekibi</li>
              <li>Detaylı raporlama</li>
            </ul>
          </div>

          <div class="model-card">
            <div class="model-icon">🏢</div>
            <h3>Outsource Mutabakat</h3>
            <p>Tüm mutabakat süreçlerinizi profesyonel ekibimize devredin.</p>
            <ul class="model-features">
              <li>Tam dış kaynak kullanımı</li>
              <li>Deneyimli mutabakat ekibi</li>
              <li>Sonuç odaklı çalışma</li>
            </ul>
          </div>

          <div class="model-card">
            <div class="model-icon">🔄</div>
            <h3>Hybrid Model</h3>
            <p>Online ve outsource modellerin karışımı ile esnek çözüm.</p>
            <ul class="model-features">
              <li>Esnek çalışma modeli</li>
              <li>İhtiyaca özel planlama</li>
              <li>Maliyet optimizasyonu</li>
            </ul>
          </div>
        </div>

        <div class="cta-banner">
          <a href={getWhatsAppLink("E-Mutabakat Teklif")} class="btn primary-btn large">
            <span class="btn-icon">📋</span>
            HEMEN TEKLİF AL
          </a>
        </div>
      </div>
    </section>

    <!-- Features Section -->
    <section class="features-section">
      <div class="container">
        <div class="features-content">
          <div class="feature-item">
            <div class="feature-icon">⚡</div>
            <div class="feature-text">
              <h3>Tek tuş ile verileriniz hızlıca aktarılır</h3>
              <p>Mevcut sisteminizden tek tıkla veri aktarımı yapabilirsiniz.</p>
            </div>
          </div>

          <div class="feature-item">
            <div class="feature-icon">🔌</div>
            <div class="feature-text">
              <h3>Hızlıca entegre olur</h3>
              <p>Tüm ERP veya CRM programlarına çok kolay bir şekilde entegrasyon sağlanır.</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Products Section -->
    <section class="products-section">
      <div class="container">
        <div class="section-header">
          <h2 class="section-title">Dijital Çözümlerimiz</h2>
          <p class="section-subtitle">Tüm dijital dönüşüm ihtiyaçlarınız için kapsamlı çözümler</p>
        </div>

        <div class="products-grid">
          <div class="product-category">
            <h3>Mutabakat Çözümleri</h3>
            <div class="product-list">
              <a href={getWhatsAppLink("e-Mutabakat")} class="product-item">
                <div class="product-icon">📊</div>
                <span>e-Mutabakat</span>
              </a>
              <a href={getWhatsAppLink("Online Mutabakat")} class="product-item">
                <div class="product-icon">💻</div>
                <span>Online Mutabakat</span>
              </a>
              <a href={getWhatsAppLink("Yanıt Garantili Mutabakat")} class="product-item">
                <div class="product-icon">✅</div>
                <span>Yanıt Garantili Mutabakat</span>
              </a>
              <a href={getWhatsAppLink("Hybrid Model")} class="product-item">
                <div class="product-icon">🔄</div>
                <span>Hybrid Model</span>
              </a>
              <a href={getWhatsAppLink("Outsource Mutabakat")} class="product-item">
                <div class="product-icon">🏢</div>
                <span>Outsource Mutabakat</span>
              </a>
            </div>
          </div>

          <div class="product-category">
            <h3>Program Özellikleri</h3>
            <div class="product-list">
              <a href={getWhatsAppLink("Otomatik Hatırlatma")} class="product-item">
                <div class="product-icon">🔔</div>
                <span>Otomatik Hatırlatma</span>
              </a>
              <a href={getWhatsAppLink("Raporlama Sistemi")} class="product-item">
                <div class="product-icon">📈</div>
                <span>Detaylı Raporlama</span>
              </a>
              <a href={getWhatsAppLink("API Entegrasyon")} class="product-item">
                <div class="product-icon">🔗</div>
                <span>API Entegrasyonu</span>
              </a>
              <a href={getWhatsAppLink("Çoklu Kullanıcı")} class="product-item">
                <div class="product-icon">👥</div>
                <span>Çoklu Kullanıcı</span>
              </a>
            </div>
          </div>

          <div class="product-category">
            <h3>Dijital Fatura Çözümleri</h3>
            <div class="product-list">
              <a href={getWhatsAppLink("e-Fatura")} class="product-item">
                <div class="product-icon">🧾</div>
                <span>e-Fatura</span>
              </a>
              <a href={getWhatsAppLink("e-Arşiv Fatura")} class="product-item">
                <div class="product-icon">📁</div>
                <span>e-Arşiv Fatura</span>
              </a>
              <a href={getWhatsAppLink("e-İrsaliye")} class="product-item">
                <div class="product-icon">📦</div>
                <span>e-İrsaliye</span>
              </a>
              <a href={getWhatsAppLink("e-Defter")} class="product-item">
                <div class="product-icon">📚</div>
                <span>e-Defter</span>
              </a>
              <a href={getWhatsAppLink("e-Serbest Meslek Makbuzu")} class="product-item">
                <div class="product-icon">💼</div>
                <span>e-Serbest Meslek Makbuzu</span>
              </a>
              <a href={getWhatsAppLink("e-Müstahsil Makbuzu")} class="product-item">
                <div class="product-icon">👨‍🌾</div>
                <span>e-Müstahsil Makbuzu</span>
              </a>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Benefits Section -->
    <section class="benefits-section">
      <div class="container">
        <div class="section-header">
          <h2 class="section-title">e-Mutabakat ile Kazanımlarınız</h2>
        </div>

        <div class="benefits-grid">
          <div class="benefit-card">
            <div class="benefit-icon">⏱️</div>
            <h3>Zaman Tasarrufu</h3>
            <p>Manuel mutabakat süreçlerine göre %90'a varan zaman tasarrufu sağlayın.</p>
          </div>
          <div class="benefit-card">
            <div class="benefit-icon">💰</div>
            <h3>Maliyet Azaltma</h3>
            <p>Yanıt alınmayan mutabakatlardan doğan gereksiz ödemeleri ortadan kaldırın.</p>
          </div>
          <div class="benefit-card">
            <div class="benefit-icon">📊</div>
            <h3>Detaylı Raporlama</h3>
            <p>Anlık raporlar ile mutabakat süreçlerinizi takip edin ve analiz edin.</p>
          </div>
          <div class="benefit-card">
            <div class="benefit-icon">🔒</div>
            <h3>Güvenlik</h3>
            <p>Bankalar ile aynı güvenlik standartlarında şifreleme ile verileriniz güvende.</p>
          </div>
          <div class="benefit-card">
            <div class="benefit-icon">🔄</div>
            <h3>Otomasyon</h3>
            <p>Otomatik hatırlatma ve takip sistemleri ile süreçlerinizi otomatikleştirin.</p>
          </div>
          <div class="benefit-card">
            <div class="benefit-icon">📱</div>
            <h3>Mobil Erişim</h3>
            <p>İstediğiniz yerden, istediğiniz zaman mutabakatlarınızı takip edin.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Integration Section -->
    <section class="integration-section">
      <div class="container">
        <div class="section-header">
          <h2 class="section-title">Entegrasyon ve Uyumluluk</h2>
          <p class="section-subtitle">Tüm sistemlerinizle uyumlu çalışır</p>
        </div>

        <div class="integration-grid">
          <div class="integration-item">
            <div class="integration-icon">💼</div>
            <h3>ERP Sistemleri</h3>
            <p>SAP, Logo, Mikro, Oracle, Microsoft Dynamics</p>
          </div>
          <div class="integration-item">
            <div class="integration-icon">🤝</div>
            <h3>CRM Sistemleri</h3>
            <p>Salesforce, HubSpot, Zoho CRM, Microsoft CRM</p>
          </div>
          <div class="integration-item">
            <div class="integration-icon">🏦</div>
            <h3>Banka Sistemleri</h3>
            <p>Tüm bankaların internet şubeleri ile entegrasyon</p>
          </div>
          <div class="integration-item">
            <div class="integration-icon">📊</div>
            <h3>Muhasebe Yazılımları</h3>
            <p>Eta, Link, Luca, Netsoft ve diğer tüm yazılımlar</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Contact Section -->
    <section id="contact" class="contact-section">
      <div class="container">
        <div class="contact-content">
          <div class="contact-info">
            <h2 class="contact-title">1 Günde Dijital Dönüşüm</h2>
            <p class="contact-description">
              e-Mutabakat sistemimize geçişiniz sadece 1 Gün sürüyor. 
              Teknik ekibimiz sürecin her adımında yanınızda olacak.
            </p>
            <div class="contact-features">
              <div class="contact-feature">
                <div class="feature-check">✅</div>
                <span>Demo ve eğitim</span>
              </div>
              <div class="contact-feature">
                <div class="feature-check">✅</div>
                <span>7/24 teknik destek</span>
              </div>
              <div class="contact-feature">
                <div class="feature-check">✅</div>
                <span>Veri aktarım garantisi</span>
              </div>
              <div class="contact-feature">
                <div class="feature-check">✅</div>
                <span>Hemen ve kolay kullanım</span>
              </div>
            </div>
          </div>
          <div class="contact-form">
            <div class="form-header">
              <h3>Demo Talep Edin</h3>
              <p>Size özel teklifimiz için hemen iletişime geçin</p>
            </div>
            <div class="contact-buttons">
              <a href={getWhatsAppLink("e-Mutabakat Demo Talebi")} class="btn whatsapp-btn">
                <span class="btn-icon">💬</span>
                WhatsApp ile İletişim
              </a>
              <a href="/iletisim" class="btn phone-btn">
                <span class="btn-icon">📞</span>
                Bizi Arayın
              </a>
              <a href="mailto:info@e-mutabakat.com" class="btn email-btn">
                <span class="btn-icon">✉️</span>
                E-posta Gönderin
              </a>
            </div>
            <div class="form-note">
              <p>📞 <strong>Hızlı Destek:</strong> Çalışma saatlerimiz içinde 15 dakikada dönüş garantisi</p>
            </div>
          </div>
        </div>
      </div>
    </section>
  </div>

  <style is:global>
    :root {
      --mutabakat-blue: #1d4ed8;
      --dark-blue: #1e40af;
      --light-blue: #dbeafe;
      --discount-red: #dc2626;
      --success-green: #10b981;
      --warning-orange: #f59e0b;
      --dark-bg: #0f172a;
      --text-color: #1f2937;
      --text-light: #6b7280;
      --border-color: #e5e7eb;
      --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
      --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
      --mutabakat-gradient: linear-gradient(135deg, #1d4ed8, #3b82f6);
    }

    .e-mutabakat-page {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
      color: var(--text-color);
      line-height: 1.6;
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 1rem;
    }

    /* Hero Section */
    .hero-section {
      background: var(--mutabakat-gradient);
      color: white;
      padding: 5rem 1rem;
      position: relative;
      overflow: hidden;
    }

    .hero-section .container {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 4rem;
      align-items: center;
    }

    .hero-title {
      font-size: 3.2rem;
      font-weight: 800;
      line-height: 1.2;
      margin-bottom: 1rem;
    }

    .e-text {
      color: white;
      display: block;
      font-size: 4rem;
      background: linear-gradient(45deg, #ffffff, #dbeafe);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .tagline {
      color: #dbeafe;
      display: block;
      font-size: 1.8rem;
      font-weight: 600;
    }

    .hero-subtitle {
      font-size: 1.2rem;
      color: #dbeafe;
      margin-bottom: 2rem;
      line-height: 1.7;
    }

    .hero-actions {
      display: flex;
      gap: 1rem;
      flex-wrap: wrap;
    }

    .btn {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      padding: 1rem 2rem;
      border-radius: 8px;
      text-decoration: none;
      font-weight: 600;
      transition: all 0.3s ease;
      border: none;
      cursor: pointer;
      font-size: 1rem;
    }

    .primary-btn {
      background: white;
      color: var(--mutabakat-blue);
      box-shadow: var(--shadow);
    }

    .primary-btn:hover {
      transform: translateY(-2px);
      box-shadow: var(--shadow-lg);
      background: #f8fafc;
    }

    .secondary-btn {
      background: transparent;
      color: white;
      border: 2px solid white;
    }

    .secondary-btn:hover {
      background: white;
      color: var(--mutabakat-blue);
      transform: translateY(-2px);
    }

    .large {
      padding: 1.2rem 2.5rem;
      font-size: 1.1rem;
    }

    .hero-illustration {
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .mutabakat-animation {
      display: flex;
      align-items: center;
      gap: 2rem;
    }

    .document-icon, .check-icon, .sync-icon {
      font-size: 4rem;
      animation: bounce 2s ease-in-out infinite;
    }

    .document-icon { animation-delay: 0s; }
    .check-icon { animation-delay: 0.3s; }
    .sync-icon { animation-delay: 0.6s; }

    @keyframes bounce {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-20px); }
    }

    /* Discount Section */
    .discount-section {
      background: linear-gradient(135deg, #fee2e2, #fecaca);
      padding: 2rem 1rem;
    }

    .discount-content {
      display: grid;
      grid-template-columns: auto 1fr auto;
      gap: 2rem;
      align-items: center;
      max-width: 1000px;
      margin: 0 auto;
    }

    .discount-badge {
      background: var(--discount-red);
      color: white;
      padding: 1.5rem;
      border-radius: 10px;
      text-align: center;
      min-width: 120px;
      box-shadow: var(--shadow);
    }

    .discount-text {
      display: block;
      font-size: 2.5rem;
      font-weight: 800;
    }

    .discount-label {
      display: block;
      font-size: 1rem;
      font-weight: 600;
    }

    .discount-info h2 {
      font-size: 1.8rem;
      color: var(--discount-red);
      margin-bottom: 0.5rem;
    }

    .discount-info p {
      color: var(--text-color);
      line-height: 1.6;
    }

    .discount-btn {
      background: var(--discount-red);
      color: white;
      box-shadow: var(--shadow);
    }

    .discount-btn:hover {
      background: #b91c1c;
      transform: translateY(-2px);
      box-shadow: var(--shadow-lg);
    }

    /* About Section */
    .about-section {
      padding: 4rem 1rem;
      background: white;
    }

    .about-header {
      text-align: center;
      margin-bottom: 3rem;
    }

    .section-title {
      font-size: 2.5rem;
      font-weight: 700;
      margin-bottom: 1rem;
      color: var(--mutabakat-blue);
    }

    .company-info {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 0.5rem;
    }

    .company-logo {
      font-size: 1.5rem;
      font-weight: 700;
      color: var(--mutabakat-blue);
      letter-spacing: 1px;
    }

    .company-tagline {
      font-size: 1.2rem;
      color: var(--text-light);
      font-style: italic;
    }

    .stats-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 1.5rem;
      margin-bottom: 3rem;
    }

    .stat-card {
      background: var(--light-blue);
      padding: 2rem;
      border-radius: 10px;
      text-align: center;
      transition: all 0.3s ease;
    }

    .stat-card:hover {
      transform: translateY(-5px);
      box-shadow: var(--shadow);
    }

    .stat-number {
      font-size: 2.5rem;
      font-weight: 800;
      color: var(--mutabakat-blue);
      margin-bottom: 0.5rem;
    }

    .stat-title {
      font-size: 1.1rem;
      color: var(--text-color);
      font-weight: 600;
    }

    .warning-banner {
      background: #fffbeb;
      border: 2px solid var(--warning-orange);
      border-radius: 10px;
      padding: 1.5rem;
      display: flex;
      align-items: center;
      gap: 1rem;
      max-width: 800px;
      margin: 0 auto;
    }

    .warning-icon {
      font-size: 2rem;
      color: var(--warning-orange);
    }

    .warning-content h3 {
      color: var(--text-color);
      margin-bottom: 0.5rem;
    }

    .warning-content p {
      color: var(--text-light);
    }

    /* Models Section */
    .models-section {
      padding: 6rem 1rem;
      background: #f8fafc;
    }

    .section-header {
      text-align: center;
      margin-bottom: 3rem;
    }

    .section-subtitle {
      font-size: 1.2rem;
      color: var(--text-light);
      max-width: 600px;
      margin: 0 auto;
    }

    .models-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 2rem;
      max-width: 1200px;
      margin: 0 auto 3rem;
    }

    .model-card {
      background: white;
      padding: 2rem;
      border-radius: 15px;
      box-shadow: var(--shadow);
      transition: all 0.3s ease;
      border-top: 4px solid transparent;
    }

    .model-card:hover {
      transform: translateY(-5px);
      box-shadow: var(--shadow-lg);
    }

    .model-card.featured {
      border-color: var(--mutabakat-blue);
      background: linear-gradient(135deg, #f8fafc, var(--light-blue));
    }

    .model-icon {
      font-size: 3rem;
      margin-bottom: 1rem;
      color: var(--mutabakat-blue);
    }

    .model-card h3 {
      font-size: 1.3rem;
      margin-bottom: 1rem;
      color: var(--text-color);
    }

    .model-card p {
      color: var(--text-light);
      margin-bottom: 1.5rem;
      line-height: 1.6;
    }

    .model-features {
      list-style: none;
      padding: 0;
      margin: 0;
    }

    .model-features li {
      padding: 0.5rem 0;
      color: var(--text-color);
      position: relative;
      padding-left: 1.5rem;
    }

    .model-features li:before {
      content: "✓";
      color: var(--success-green);
      position: absolute;
      left: 0;
      font-weight: bold;
    }

    .cta-banner {
      text-align: center;
      padding-top: 2rem;
      border-top: 1px solid var(--border-color);
    }

    /* Features Section */
    .features-section {
      padding: 4rem 1rem;
      background: white;
    }

    .features-content {
      max-width: 800px;
      margin: 0 auto;
      display: grid;
      gap: 2rem;
    }

    .feature-item {
      display: flex;
      align-items: flex-start;
      gap: 1.5rem;
      padding: 2rem;
      background: #f8fafc;
      border-radius: 10px;
      border-left: 4px solid var(--mutabakat-blue);
    }

    .feature-icon {
      font-size: 2rem;
      color: var(--mutabakat-blue);
    }

    .feature-text h3 {
      font-size: 1.3rem;
      margin-bottom: 0.5rem;
      color: var(--text-color);
    }

    .feature-text p {
      color: var(--text-light);
    }

    /* Products Section */
    .products-section {
      padding: 6rem 1rem;
      background: #f8fafc;
    }

    .products-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 2rem;
      max-width: 1200px;
      margin: 0 auto;
    }

    .product-category {
      background: white;
      padding: 2rem;
      border-radius: 15px;
      box-shadow: var(--shadow);
    }

    .product-category h3 {
      font-size: 1.4rem;
      color: var(--mutabakat-blue);
      margin-bottom: 1.5rem;
      padding-bottom: 1rem;
      border-bottom: 2px solid var(--light-blue);
    }

    .product-list {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    .product-item {
      display: flex;
      align-items: center;
      gap: 1rem;
      padding: 1rem;
      background: #f8fafc;
      border-radius: 8px;
      text-decoration: none;
      color: var(--text-color);
      transition: all 0.3s ease;
    }

    .product-item:hover {
      background: var(--light-blue);
      transform: translateX(5px);
    }

    .product-icon {
      font-size: 1.5rem;
    }

    .product-item span {
      font-weight: 500;
    }

    /* Benefits Section */
    .benefits-section {
      padding: 6rem 1rem;
      background: white;
    }

    .benefits-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 2rem;
      max-width: 1200px;
      margin: 0 auto;
    }

    .benefit-card {
      text-align: center;
      padding: 2rem;
      background: #f8fafc;
      border-radius: 15px;
      transition: all 0.3s ease;
    }

    .benefit-card:hover {
      transform: translateY(-5px);
      box-shadow: var(--shadow);
    }

    .benefit-icon {
      font-size: 2.5rem;
      margin-bottom: 1rem;
      color: var(--mutabakat-blue);
    }

    .benefit-card h3 {
      font-size: 1.2rem;
      margin-bottom: 1rem;
      color: var(--text-color);
    }

    .benefit-card p {
      color: var(--text-light);
      font-size: 0.9rem;
      line-height: 1.5;
    }

    /* Integration Section */
    .integration-section {
      padding: 6rem 1rem;
      background: #f8fafc;
    }

    .integration-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 2rem;
      max-width: 1200px;
      margin: 0 auto;
    }

    .integration-item {
      text-align: center;
      padding: 2rem;
      background: white;
      border-radius: 15px;
      box-shadow: var(--shadow);
      transition: all 0.3s ease;
    }

    .integration-item:hover {
      transform: translateY(-5px);
      box-shadow: var(--shadow-lg);
    }

    .integration-icon {
      font-size: 2.5rem;
      margin-bottom: 1rem;
      color: var(--mutabakat-blue);
    }

    .integration-item h3 {
      font-size: 1.2rem;
      margin-bottom: 1rem;
      color: var(--text-color);
    }

    .integration-item p {
      color: var(--text-light);
      font-size: 0.9rem;
      line-height: 1.5;
    }

    /* Contact Section */
    .contact-section {
      padding: 6rem 1rem;
      background: var(--mutabakat-gradient);
      color: white;
    }

    .contact-content {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 4rem;
      align-items: center;
      max-width: 1200px;
      margin: 0 auto;
    }

    .contact-title {
      font-size: 2.5rem;
      margin-bottom: 1rem;
    }

    .contact-description {
      font-size: 1.1rem;
      color: #dbeafe;
      margin-bottom: 2rem;
      line-height: 1.6;
    }

    .contact-features {
      display: grid;
      gap: 1rem;
    }

    .contact-feature {
      display: flex;
      align-items: center;
      gap: 1rem;
    }

    .feature-check {
      color: var(--success-green);
    }

    .contact-form {
      background: white;
      padding: 2.5rem;
      border-radius: 15px;
      box-shadow: var(--shadow-lg);
    }

    .form-header {
      text-align: center;
      margin-bottom: 2rem;
    }

    .form-header h3 {
      color: var(--mutabakat-blue);
      font-size: 1.8rem;
      margin-bottom: 0.5rem;
    }

    .form-header p {
      color: var(--text-light);
    }

    .contact-buttons {
      display: grid;
      gap: 1rem;
    }

    .whatsapp-btn {
      background: #25D366;
      color: white;
    }

    .whatsapp-btn:hover {
      background: #128C7E;
    }

    .phone-btn {
      background: var(--mutabakat-blue);
      color: white;
    }

    .phone-btn:hover {
      background: var(--dark-blue);
    }

    .email-btn {
      background: #f8fafc;
      color: var(--mutabakat-blue);
      border: 1px solid var(--border-color);
    }

    .email-btn:hover {
      background: var(--light-blue);
    }

    .form-note {
      margin-top: 2rem;
      padding: 1rem;
      background: #f0f9ff;
      border-radius: 8px;
      border-left: 4px solid var(--mutabakat-blue);
    }

    .form-note p {
      color: var(--text-color);
      font-size: 0.9rem;
    }

    /* Responsive Design */
    @media (max-width: 768px) {
      .hero-section .container,
      .contact-content {
        grid-template-columns: 1fr;
        gap: 2rem;
      }

      .hero-title {
        font-size: 2.5rem;
      }

      .e-text {
        font-size: 3rem;
      }

      .discount-content {
        grid-template-columns: 1fr;
        text-align: center;
      }

      .hero-actions {
        flex-direction: column;
      }

      .btn {
        width: 100%;
        justify-content: center;
      }

      .models-grid,
      .products-grid,
      .benefits-grid,
      .integration-grid {
        grid-template-columns: 1fr;
      }

      .features-content {
        grid-template-columns: 1fr;
      }

      .feature-item {
        flex-direction: column;
        text-align: center;
      }

      .contact-buttons {
        grid-template-columns: 1fr;
      }
    }
  </style>

  <script>
    document.addEventListener('DOMContentLoaded', function() {
      // Smooth scrolling for anchor links
      document.querySelectorAll('a[href^="#"]').forEach(anchor => {
        anchor.addEventListener('click', function(e) {
          e.preventDefault();
          
          const targetId = this.getAttribute('href');
          if (targetId === '#') return;
          
          const targetElement = document.querySelector(targetId);
          if (targetElement) {
            window.scrollTo({
              top: targetElement.offsetTop - 80,
              behavior: 'smooth'
            });
          }
        });
      });

      // Product item hover effects
      document.querySelectorAll('.product-item').forEach(item => {
        item.addEventListener('mouseenter', function() {
          this.style.transform = 'translateX(5px)';
        });
        
        item.addEventListener('mouseleave', function() {
          this.style.transform = 'translateX(0)';
        });
      });
    });
  </script>
</BasicLayout>